// Set this to the complete HTTPS endpoint for the deployed backend proxy.
// Example: https://your-service.onrender.com/api/messages
window.RESUME_GENIUS_CONFIG = Object.freeze({
  apiEndpoint: "",
});
